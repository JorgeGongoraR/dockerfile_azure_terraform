# Dockerfile centos_azurecli_terraform
Dockerfile to create a image with azure cli and terraform on CentOs.

### How to build?
```shell
docker build -t repositoty_name:tag .
```
### How to run?
```shell
docker run -it repositoty_name:tag
```
